/** pt-BR practice lesson URL — keep in sync with app router folders. */
export function practiceLessonPath(categoryPath: string, slug: string): string {
  return `/pt-br/pratica/${categoryPath}/${slug}`;
}
